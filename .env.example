# Example environment file for Qguardarr
# Copy this to .env and update values for your setup

# qBittorrent connection
# For Docker on Mac/Windows, use host.docker.internal
# For Docker on Linux, use localhost or the actual IP
QBIT_HOST=host.docker.internal
QBIT_PORT=8080
QBIT_USERNAME=admin
QBIT_PASSWORD=your_password_here

# Cross-seed integration (optional)
# Leave empty to disable cross-seed forwarding
CROSS_SEED_URL=http://host.docker.internal:2468/api/webhook
CROSS_SEED_API_KEY=your_api_key_here

# Application settings (optional - defaults will be used if not set)
# GLOBAL_UPDATE_INTERVAL=300
# GLOBAL_ROLLOUT_PERCENTAGE=10
# GLOBAL_HOST=0.0.0.0
# GLOBAL_PORT=8089

# Logging settings (optional)
# LOG_LEVEL=INFO
# LOG_FILE=./logs/qguardarr.log
